apiVersion: serving.knative.dev/v1alpha1
kind: Service
metadata:
  name: nginx-gloo
  namespace: default
spec:
  runLatest:
    configuration:
      revisionTemplate:
        spec:
          container:
            image: nginx:1
            ports:
              - containerPort: 80
            env:
              - name: PORT
                value: "80"

